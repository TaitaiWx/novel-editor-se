一期

目标:

1. 软件可以完整使用，正常支持作者、编剧等角色的业务需求和使用
2. 可以正常保存、导出文本、转换格式

- [ ] 搭建整体框架
  - [ ] 具体的组件、页面拆分
- [ ] 完善具体页面的框架

  07.06

- [x] loading 展示的组件拆分
- [x] react-icon 这些icon 的颜色搭配不是很对。现在是黑色，应该是偏白色才对
- [x] 展示内容的地方，样式要有分割线
  - [x] 方格子
  - [x] 行线
  - [x] 边界case
- [x] UI 样式

  - [x] 动画抽象化
  - [x] 统一结构化
  - [x] 加载状态组件统一
  - [x] 错误状态组件统一

    07.08

- [ ] 代码阅读器拆分成专门的lib(这块目前不是重点)
  - [ ] 未来会改成单独的插件模式来引入
  - [x] 主要侧重文案工作，而不是代码编辑器
- [x] 现在只能查看文件，不能修改文件内容
- [x] 内容展示的样式需要调整
  - [x] 行号需要支持高亮当前行
- [x] 修复IPC权限问题（write-file频道）
- [x] 实现自动保存功能（2秒延迟自动保存）
- [x] 实现当前行高亮效果
- [x] 替换手动保存为自动保存机制
- [x] 修复文件切换时的自动保存bug（防止内容覆盖其他文件）
- [x] 优化自动保存逻辑，确保文件切换时正确保存到原文件
- [x] 完善组件卸载时的文件保存机制
- [x] 自动保存优化完成

  - 文件切换时正确保存到原文件，避免内容错乱
  - 使用 ref 追踪当前文件状态，防止异步问题
  - 组件卸载时安全保存当前文件
  - 所有保存操作都有错误处理和状态指示

    07.09

- [x] UI - 追求跨端一致
  - [x] 去掉软件的 header
  - [x] Header 使用自定义(最小化、全屏、关闭)，查看 win, mac 跨端的效果
  - [x] windows 去掉 tab toolbar
  - [x] macOS 完全隐藏系统按钮，只显示自定义按钮
  - [x] 自定义 macOS 风格的圆形控制按钮（红、黄、绿）
  - [x] 调整按钮大小和图标比例，优化视觉效果
  - [x] 统一三端的标题栏样式和交互
  - [x] 使用 React Icons 替换字符图标，提升视觉质量
- [x] 功能

  - [x] 代码逻辑梳理、优化
  - [x] 快捷键操作

    07.10

- [ ] 功能
  - [ ] 代码逻辑梳理、优化
  - [ ] 支持 chm 文件阅读
  - [ ] 小说(作品)集合
  - [ ] 章节控制、集合
  - [ ] 程序界面下方添加status bar: 今日输入总字数，当前文档今日输入总字数，今日使用时间，今日有效使用时间。当前文档总行数，总字数，当前光标所在位置（行，列）
  - [ ] 添加大纲视图，根据段落，标题生成大纲，方便导航

检查下面的所有实现，是否都实现了？是否是业界最高性能的成熟实现？不是的话，就优化实现，注释都改成中文

1. 快捷键提示里提示的，实际上很多都没有实现，两边没有对齐。配置的快捷键+键盘快捷键应该是一致的。键盘快捷键展示应该是全部.command+q 都无法退出
2. 是不是要支持 sqlite，导出的数据可以是data这种结构化的，或者版本管理，文件管理等等，这样方便迁移。保证了既能打开本地的文件目录，也能自己维护 sqlite 里的数据，方便用户管理和迁移数据，同时也能提供更好的性能和稳定性。可以考虑使用一些成熟的 sqlite 库，比如 sqlite3、better-sqlite3 等，来实现对 sqlite 数据库的操作，同时也要注意数据的安全性和隐私保护，确保用户的数据不会被泄露或者滥用。
3. 写 github-action 来自动化测试和部署，确保每次提交代码后都能自动运行测试用例，保证代码的质量和稳定性，同时也可以自动部署到生产环境，提高开发效率和发布速度。ci 是检测现在代码是否有报错, delpoy 是把现在的代码编译成可执行文件，要有 windows/macos/linux 三个平台的版本，和 arm64 / x86 / x64，发布到 github release 上，方便用户下载和使用
4. 现在大纲的生成的算法有点烂，是根据正则来匹配的，比如章、回等，这个不是很准确吧？还可以优化吗？有啥算法吗？

可以考虑引入一些自然语言处理的技术，比如分词、命名实体识别等，来更准确地生成大纲结构，这样可以提升大纲的准确性和用户体验，同时也能更好地适应不同类型的文本内容。可以把这些算法实现放在 basic-algorithm 里，提供给其他库调用，避免重复实现，同时也能保证算法的性能和稳定性。

5. 清理 textarea 相关的死代码（虚拟化行号、手动滚动同步等）
6. 实现自定义写作 UI（如场景标记、人物高亮等）
7. 保留现有功能（自动保存、行号、行高亮、方格线/行线、编码切换、快捷键）
8. Diff Editor（内置文件对比）— 版本变化、修订功能等地方使用，可以独立实现

---

#codebase

1. 快捷键依旧是没有， cmd +q 依旧不能退出。 快捷键提示里提示的，实际上很多都没有实现，两边没有对齐。配置的快捷键+键盘快捷键应该是一致的。键盘快捷键展示应该是全部，command+q 都无法退出。 header 的布局管理优化下， 专注模式应该居中，键盘快捷键应该是属于设置里的，但是又要快速的提醒用户这里有快捷键操作
2. 大纲算法是怎么实现的？仔细讲解，最后是写到 docs 里的新 md 中，都有几种算法，怎么用的，为啥不用自然语言处理
3. sqlite 存储是做啥的？为啥不集成？
4. 自定义写作 UI都有什么功能？仔细讲解写到 docs 里的 md 中
5. Diff Editor 如何使用？是怎么设计实现的？怎么查看呢？是否要创建个例子文档，然后给我查看？版本控制是放到 editor 里，还是放到 footer 里展示时间线保存呢？ 设计思路写到 docs 的 md 中
6. 当前是不是少了版本管理功能（如 git 集成、版本历史查看等），要考虑里面可能会有音频、二进制等等文件，版本管理功能需要支持这些文件的版本控制和历史查看，可以考虑使用一些成熟的版本控制系统，比如 git、svn 等，来实现版本管理功能，同时也要注意数据的安全性和隐私保护，确保用户的数据不会被泄露或者滥用。版本管理功能可以集成到编辑器中，提供一个版本历史查看的界面，让用户可以方便地查看和管理不同版本的文件，同时也可以提供一些版本比较和合并的功能，方便用户进行版本控制和协作。要考虑 git 和 svn 不适合保留大量素材或者是者是二进制文件的版本控制，可以考虑使用一些专门的版本控制系统，比如 git-lfs、svn-largefiles 等，来实现对这些文件的版本控制和历史查看
7. 文件管理功能（如文件夹结构展示、文件搜索等）。文件管理功能可以提供一个文件夹结构展示的界面，让用户可以方便地浏览和管理自己的文件，同时也可以提供一个文件搜索功能，方便用户快速找到需要的文件。可以考虑使用一些成熟的文件管理库，比如 react-file-tree、react-file-explorer 等，来实现文件管理功能，同时也要注意数据的安全性和隐私保护，确保用户的数据不会被泄露或者滥用。
8. 是否是业界最高性能的成熟实现？不是的话，就优化实现，注释都改成中文

---

#codebase

1. 专注模式，是header 里的这个点击的地方，不是editor居中！实现不对，editor 应该是直接full-width
2. 文件搜索到之后应该是自动打开目录，聚焦到文件
3. 版本管理没有引入集成。版本控制是放到 editor 里，还是放到 footer 里展示时间线保存呢？

---

1. 添加一些图片、pdf、二进制等的sample啊，方便我能查看功能和对比才对。尤其是上面实现的功能
2. 实现音频等剩余二进制资源的专用预览器
3. 现在的版本回溯，好像是看不到具体哪些地方修改了，没有高亮等的展示，是不是优化下
4. 优化下渲染包体积仍有 chunk size warning

5. 给图片和 PDF 做版本预览，而不是只提示“不可文本 Diff”
6. 增加“恢复到该版本”能力，把 SQLite 快照真正闭环成可回滚系统
7. 给大项目加异步快照进度条，避免首次扫描大素材目录时 UI 停顿
8. 所有必须是成熟的高性能业界实现方案
9. 检查是否有遗漏的地方

所有必须是成熟的高性能业界实现方案，检查是否有遗漏的地方没处理干净

---

#codebase

1. 现在 svg, png, pdf, media 等资源，点击都是按照文本来展示的，这不友好, 点击默认是展示预览效果，除非选择菜单里的展示内容才是展示文本吧？
2. 现在 pdf 的时间线的对比展示，没有和 svg 等一样的设计，得优化下，不是跳出来个新的 pdf
3. 现在的测试展示的 png 都是无法正常打开的，处理下，换成可以打开的

所有必须是成熟的高性能业界实现方案，检查是否有遗漏的地方没处理干净

---

1. AI 中，截图的这几块，温度改到 0.7，上下文 Token 窗口是 128k，最大输出长度是8K。添加 deepseekv3.2
2. 现在人物生成应该是 JSON 发送结果，同时要分片，直接生成人物图才对。AI 结构化辅助、设定设计不应该是现在这样，而人物应该设计再大胆点

关于拆法，我感觉你说的很对，

1. 幕剧应该是有自己的结构。有自己的升级，负责结构，可以编排处理等等，做成卡片吧？而且这些区域可以调整？
2. 设定集又该放在哪里呢？AI 未来要引用，也要给建议，说哪里不对，要给指导，辅助我们整理数据等等
3. 人物系统这块，我更喜欢是直观的可视化，把内容设计都展示出来。你说的几个差异化点，是我觉得可以的，市场也会认同
4. AI 应该是一个结构化辅助面板，也可以提供助手的形式，指导和方便用户来询问和获取帮助等
5. 你对AI区域，以及AI真正价值的定位很准确，我很认同。

## 请按照我们说的演进之路开始处理和优化，弥补产品的缺口

现在的设计风格和方向我不是很满意，

1. 人物、情节、设定、AI 都应该优化下视觉效果，需要突出特色，和其他产品独立开
2. 大纲节点可以直接拖拽重排对应正文，但是大纲是纲要，指明方向
3. 卡片粒度我更倾向去做场景级别，这个才更符合剧本的实现现状，尤其是《故事》 里的定位
4. 但是我不确定要自由画布，还是时间线，还是也保留传统？因为过于超前的设计，会带来麻烦，客户可能不太喜欢？但是我觉得这也是用产品培养客户的过程。时间线上的故事卡片，最后查看是一个正常传统的文章，人物用自用画布那种
5. 但我不确定，需要你帮我确定这些产品的需求

检查下现在的实现，是业界成熟的最佳实践吗？是最高性能的算法吗？按照成熟高效的拆分逻辑拆出来，未来可以轻松复用吗？

注意：

1. 这是数据驱动，必须保证从根源来解决问题，而不是依靠传入的传入这种间接方式来处理
2. 必须是业界成熟的最佳实践，用最高性能的算法，按照成熟高效的拆分逻辑拆出来，未来可以轻松复用
3. 每次处理完之前，都要运行 lint, test 检测是否有报错，然后进行修复

---

Phase 1（当前可做）：

合并大纲 + 情节为「故事线」tab
AI tab 降级为各模块的内嵌按钮
Tab 减为 3 个：故事线 / 人物 / 设定
场景卡片 UI 优化（目标/冲突/结果的结构化输入）, 场景卡片作为故事线的核心交互单位。
Phase 2（验证后做）：

场景拖拽 → 正文重排联动
泳道时间线布局（替代当前的列表式）
因果链连线
Phase 3（用户反馈后做）：

人物弧光时间轴， 节拍级拆解， 多线叙事泳道

待做列表
Phase 2 — 故事蓝图增强

因果链可视化（场景之间的因果连线）
场景卡增强：加节拍（beat）字段、人物转折标注
场景卡拖拽重排 → 同步正文段落位置
缺口/断链检测提示（检测伏笔未回收、因果断裂）
泳道时间线布局（按人物/POV 分泳道展示场景）
Phase 3 — 深度结构 6. 人物弧线时间线（跨场景追踪角色状态变化）7. Beat 级细拆（场景内的节拍展开与编辑）8. 多 POV 泳道并排视图 9. AI 助手对话框增强（上下文记忆、多轮对话）

---

useMessagePort<TSend, TReceive>(channelName, onMessage?) 是完全通用的抽象，未来可直接用于：

协同编辑实时同步
每帧动画数据传输
任意两个窗口间的高频通信
支持 Transferable（ArrayBuffer 零拷贝传输）

---

注意：

1. 实现必须是业界成熟、最高性能的最佳实践
2. 注释必须是中文
3. 获取的数据源必须是从核心、根源的地方获取，比如 redux 或者 sqlite 等，禁止从组件的 state 里获取，避免数据不一致或者是者是异步问题导致的 bug

---

本轮检查与修复

- [x] ✅ 修复“提取后自动生成空资料目录”的问题。根因是 AI 资料落盘流程会先创建目录，再写文件；现在改为只有成功写出文件时才保留自动创建的目录。
- [x] ✅ 角色增加“主要角色 / 次要角色”分类，并从 SQLite 根数据统一读取，在人物页和侧栏同时按分类分组展示。
- [x] ✅ 支持人物、设定、资料按需单独导出或组合导出，导出数据直接来自 SQLite 与持久化资料源，避免组件状态不一致。
- [x] ✅ 角色分类增加“快速筛选”和“批量改分类”，批量更新直接写回 SQLite，避免界面分组与真实数据不一致。
- [x] ✅ 增加历史空资料目录安全清理迁移，只清理系统自动生成且为空的目录，并在打开项目时自动执行一次。
